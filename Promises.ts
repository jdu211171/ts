/*
Promises provide a cleaner way to handle asynchronous operations by using a chainable syntax. They can be in one of three states: pending, fulfilled, or rejected.
*/

function fetchData(): Promise<string>